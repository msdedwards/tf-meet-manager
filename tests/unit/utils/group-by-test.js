import groupby from 'xc-meet-manager/utils/group-by';
import { module, test } from 'qunit';

module('Unit | Utility | group-by', function () {

  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = groupby();
    assert.ok(result);
  });
});
